# Legacy source analysis

This directory contains tracked analysis of the original `fcitx5-vinput/src` tree.

- `source-annotations.md`: first pass file-by-file annotation for every C++ source/header under the original `src/` directory.

These files are intentionally tracked because they are part of the migration record. Local scratch plans remain ignored under `docs/plan/`.
